# Production environment variables
# Copy to .env and fill in values

# Database
POSTGRES_USER=mono_user
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=mono_db

# Container Registry
REGISTRY=ghcr.io
IMAGE_PREFIX=your-org/mono
TAG=latest

# LangChain / OpenAI
OPENAI_API_KEY=
LANGCHAIN_API_KEY=
LANGCHAIN_TRACING_V2=true
LANGCHAIN_PROJECT=mono-production
